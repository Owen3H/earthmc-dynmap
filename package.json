{
  "name": "earthmc-dynmap-plus",
  "private": true,
  "type": "module",
  "scripts": {
    "userscript": "bun build-userscript.ts",
    "extension": "bun build-extension.ts",
    "build": "bun run userscript && bun run extension"
  },
  "devDependencies": {
    "typescript": "latest",
    "archiver": "^7.0.1",
    "esbuild": "^0.27.3",
    "@types/archiver": "latest",
    "@types/node": "latest"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
