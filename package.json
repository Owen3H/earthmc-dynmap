{
  "name": "earthmc-dynmap-plus",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "git config core.hooksPath .git-hooks",
    "gen-userscript": "bunx build-userscript.ts",
    "gen-extension": "bunx build-extension.ts",
    "build": "bun run gen-userscript && bun run gen-extension"
  },
  "pre-push": ["build"],
  "devDependencies": {
    "@types/archiver": "latest",
    "@types/node": "latest",
    "archiver": "^7.0.1",
    "esbuild": "^0.27.2",
    "typescript": "latest"
  }
}
